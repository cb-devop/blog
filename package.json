{
  "name": "top-blog",
  "version": "1.0.0",
  "description": "Top Blog - A multi-project blog platform",
  "scripts": {
    "dev": "concurrently \"npm run dev:admin\" \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:admin": "cd admin && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && python manage.py runserver",
    "install:all": "cd admin && npm install && cd ../frontend && npm install",
    "build": "concurrently \"cd admin && npm run build\" \"cd frontend && npm run build\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
